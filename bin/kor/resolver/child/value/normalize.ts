/**
 * 벡터의 단위 길이 정규화입니다.
 */
export const normalize = (
	/**
	 * 정규화할 벡터입니다.
	 * - `Type.Vector.` 를 입력하면 
	 *   여기서 사용 가능한 함수를 확인할 수 있습니다.
	 */
	position: string
) => {

	return `Normalize(${position})`
}