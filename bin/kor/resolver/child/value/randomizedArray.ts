/**
 * 지정된 배열의 값을
 * 무작위 순서로 나열한 복사본입니다.
 */
export const randomizedArray = (
	/**
	 * 무작위 복사본을 만들 배열입니다.
	 * - `Type.Array.` 를 입력하면 
	 *   여기서 사용 가능한 함수를 확인할 수 있습니다.
	 */
	array: string
) => {

	return `Randomized Array(${array})`
}