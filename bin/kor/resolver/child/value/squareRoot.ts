/**
 * 지정된 값의 제곱근입니다.
 */
export const squareRoot = (
	/**
	 * 제곱근을 계산할 실수값입니다.
	 * 음수의 경우 0이 됩니다.
	 * - `Type.Number.` 를 입력하면 
	 *   여기서 사용 가능한 함수를 확인할 수 있습니다.
	 */
	value: string | number | any[]
): number => {

	// @ts-ignore
	return `Square Root(${value})`
}

