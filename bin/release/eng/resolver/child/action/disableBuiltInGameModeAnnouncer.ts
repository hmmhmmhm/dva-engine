/**
 * Disables game mode announcements from the
 * announcer until reenabled or the match ends.
 */
export const disableBuiltInGameModeAnnouncer = (
) => {

	return `Disable Built In Game Mode Announcer`
}