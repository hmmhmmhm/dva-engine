/**
 * A hero constant. Specifies one of the available
 * heroes by name in the game.
 */
export const hero = (
	/**
	 * - `Type.HeroConstant.`
	 */
	hero: string | number | any[]
) => {

	return `Hero(${hero})`
}