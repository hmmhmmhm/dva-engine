/**
 * Arccosine in degrees of the specified value.
 */
export const arccosineInDegrees = (
	/**
	 * Value - Input value for the function.
	 * - `Type.Number.` 를 입력하면 
	 *   여기서 사용 가능한 함수를 확인할 수 있습니다.
	 */
	value: string | number | any[]
): number => {

	// @ts-ignore
	return `Arccosine In Degrees(${value})`
}

