/**
 * 두 숫자 중 높은 수입니다.
 */
export const max = (
	/**
	 * - `Type.Number.`
	 */
	value1: string | number | any[],
	/**
	 * - `Type.Number.`
	 */
	value2: string | number | any[]
): number => {

	// @ts-ignore
	return `Max(${value1}, ${value2})`
}

