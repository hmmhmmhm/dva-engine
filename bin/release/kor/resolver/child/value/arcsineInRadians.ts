/**
 * 지정된 각(단위: RAD)의 아크사인 값입니다.
 */
export const arcsineInRadians = (
	/**
	 * 함수의 입력값입니다.
	 * - `Type.Number.`
	 */
	value: string | number | any[]
): number => {

	// @ts-ignore
	return `Arcsine In Radians(${value})`
}

