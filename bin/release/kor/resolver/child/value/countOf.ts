/**
 * 지정된 배열 내 요소의 수입니다.
 */
export const countOf = (
	/**
	 * - `Type.Array.` 를 입력하면 
	 *   여기서 사용 가능한 함수를 확인할 수 있습니다.
	 */
	array: string | number | any[]
): number => {

	// @ts-ignore
	return `Count Of(${array})`
}

