/**
 * 지정된 각(단위: 도)의 코사인 값입니다.
 */
export const cosineFromDegrees = (
	/**
	 * 각(단위: 도)입니다.
	 * - `Type.Number.` 를 입력하면 
	 *   여기서 사용 가능한 함수를 확인할 수 있습니다.
	 */
	angle: string | number | any[]
): number => {

	// @ts-ignore
	return `Cosine From Degrees(${angle})`
}

