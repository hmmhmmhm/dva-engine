import { 
	ValueNumberType,
 } from '../../../interface'

/**
 * 지정된 각(단위: RAD)의 코사인 값입니다.
 */
export const cosineFromRadians = (
	/**
	 * 각(단위: RAD)입니다.
	 */
	angle: ValueNumberType
) => {

	return `Cosine From Radians(${angle})`
}