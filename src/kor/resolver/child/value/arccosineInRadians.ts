import { IArccosineInRadians } from '../../../interface'

/**
 * 지정된 각(단위: RAD)의 아크코사인 값입니다.
 */
export const arccosineInRadians = (value: IArccosineInRadians) => {
	return `Arccosine In Radians(${value['value']})`
}