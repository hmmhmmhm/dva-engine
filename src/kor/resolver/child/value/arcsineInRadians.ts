import { IArcsineInRadians } from '../../../interface'

/**
 * 지정된 각(단위: RAD)의 아크사인 값입니다.
 */
export const arcsineInRadians = (value: IArcsineInRadians) => {
	return `Arcsine In Radians(${value['value']})`
}