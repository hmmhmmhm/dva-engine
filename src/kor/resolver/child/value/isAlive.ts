/**
 * 플레이어의 생존 여부 입니다.
 */
export const isAlive = (
	/**
	 * 생존 여부를 확인할 플레이어입니다.
	 * - `Player.` 또는 `Type.Player.` 를 입력하면 
	 *   여기서 사용 가능한 함수를 확인할 수 있습니다.
	 */
	player: string
) => {

	return `Is Alive(${player})`
}