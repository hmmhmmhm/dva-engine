import { 
	ValueNumberType,
 } from '../../../interface'

/**
 * 두 숫자 중 높은 수입니다.
 */
export const max = (
	value1: ValueNumberType,
	value2: ValueNumberType
) => {

	return `Max(${value1}, ${value2})`
}